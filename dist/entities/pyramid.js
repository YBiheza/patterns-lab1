export class Pyramid {
    constructor(id, apex, //вершрна
    center, vertices) {
        this.id = id;
        this.apex = apex;
        this.center = center;
        this.vertices = vertices;
    }
}
